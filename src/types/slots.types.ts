/* eslint-disable @typescript-eslint/no-explicit-any */
export type TSlots = {
  _id: any;
  roomId?: string; // Make sure ;
  roomName: string;
  roomNo: string;
  date: string;
  startTime: string;
  endTime: string;
  isBooked?: boolean;
};
